target:
  name: "forward_return_5d"
  horizon_trading_days: 5
  candidate_horizons: [5, 10, 20, 60]
  benchmark: "SPY"
  neutral_threshold: 0.005
  selection_scope: "development_only"

models:
  classical:
    - logistic_regression
    - random_forest
    - gradient_boosting
    - vol_normalized_gradient_boosting_regressor
    - tuned_gradient_boosting_regressor
  qml:
    - vqc
    - qsvm
    - qcnn
